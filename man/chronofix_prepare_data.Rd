% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{chronofix_prepare_data}
\alias{chronofix_prepare_data}
\title{Prepare data}
\usage{
chronofix_prepare_data(data, id = NULL, group = NULL)
}
\arguments{
\item{data}{A data.frame containing IDs and dates.  By
default we expect a column \code{id} (or one with the name given as
the argument \code{id}) and two or more date columns.}

\item{id}{Optional name of a column within \code{data} to use for
unique individual identifiers.}

\item{group}{Optional name of a column within \code{data} to use for
groups}
}
\value{
A data.frame, with the addition of the class attribute
\code{chronofix_data}; once created you should not modify this object.
}
\description{
Prepare data for use with \code{chronofix}. You do not have to use this
function if you name your data.frame with our standard column names
(i.e., \code{id} column containing individual IDs) as it will be called
within other functions directly.  However, you can use this to
validate your data separately or to use different column names
than the defaults.
}
